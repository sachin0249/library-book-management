# library-book-management
This C++ program manages a library book list and sorts books using two algorithms. Titles are sorted alphabetically using Insertion Sort, while publication years are sorted using Selection Sort. It stores book ID, title, and year to organize library records efficiently.
